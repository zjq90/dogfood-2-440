<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.idouban.mapper.FriendMapper">

    <!-- 通用结果映射 -->
    <resultMap id="FriendResultMap" type="com.idouban.entity.Friend">
        <id column="friend_id" property="friendId" jdbcType="INTEGER"/>
        <result column="from_user_id" property="fromUserId" jdbcType="INTEGER"/>
        <result column="to_user_id" property="toUserId" jdbcType="INTEGER"/>
        <result column="status" property="status" jdbcType="INTEGER"/>
        <result column="group_id" property="groupId" jdbcType="INTEGER"/>
        <!-- 关联对方用户信息 -->
        <association property="friendUser" javaType="com.idouban.entity.User">
            <id column="user_id" property="userId" jdbcType="INTEGER"/>
            <result column="nickname" property="nickname" jdbcType="VARCHAR"/>
            <result column="portrait" property="portrait" jdbcType="VARCHAR"/>
            <result column="signature" property="signature" jdbcType="VARCHAR"/>
        </association>
    </resultMap>

    <!-- 查询用户的关注列表 -->
    <select id="selectFollowingList" resultMap="FriendResultMap">
        SELECT f.friend_id, f.to_user_id,
               u.user_id, u.nickname, u.portrait, u.signature
        FROM friend f
        LEFT JOIN user u ON f.to_user_id = u.user_id
        WHERE f.from_user_id = #{userId} AND f.status IN (1, 2)
        ORDER BY f.friend_id DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <!-- 查询用户的粉丝列表 -->
    <select id="selectFollowerList" resultMap="FriendResultMap">
        SELECT f.friend_id, f.from_user_id AS to_user_id,
               u.user_id, u.nickname, u.portrait, u.signature
        FROM friend f
        LEFT JOIN user u ON f.from_user_id = u.user_id
        WHERE f.to_user_id = #{userId} AND f.status IN (1, 2)
        ORDER BY f.friend_id DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <!-- 查询用户的好友列表（双向关注） -->
    <select id="selectFriendList" resultMap="FriendResultMap">
        SELECT f.friend_id, f.to_user_id,
               u.user_id, u.nickname, u.portrait, u.signature
        FROM friend f
        LEFT JOIN user u ON f.to_user_id = u.user_id
        WHERE f.from_user_id = #{userId} AND f.status = 2
        ORDER BY f.friend_id DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <!-- 查询黑名单列表 -->
    <select id="selectBlacklist" resultMap="FriendResultMap">
        SELECT f.friend_id, f.to_user_id,
               u.user_id, u.nickname, u.portrait, u.signature
        FROM friend f
        LEFT JOIN user u ON f.to_user_id = u.user_id
        WHERE f.from_user_id = #{userId} AND f.status = 3
        ORDER BY f.friend_id DESC
    </select>

    <!-- 查询两个用户之间的关系 -->
    <select id="selectRelation" resultType="com.idouban.entity.Friend">
        SELECT friend_id, from_user_id, to_user_id, status, group_id
        FROM friend
        WHERE from_user_id = #{fromUserId} AND to_user_id = #{toUserId}
        LIMIT 1
    </select>

    <!-- 新增关注关系 -->
    <insert id="insertFriend" useGeneratedKeys="true" keyProperty="friendId">
        INSERT INTO friend (from_user_id, to_user_id, status)
        VALUES (#{fromUserId}, #{toUserId}, #{status})
    </insert>

    <!-- 更新好友关系状态 -->
    <update id="updateFriendStatus">
        UPDATE friend
        SET status = #{status}
        WHERE from_user_id = #{fromUserId} AND to_user_id = #{toUserId}
    </update>

    <!-- 删除好友关系 -->
    <delete id="deleteFriend">
        DELETE FROM friend
        WHERE from_user_id = #{fromUserId} AND to_user_id = #{toUserId}
    </delete>

    <!-- 查询关注数 -->
    <select id="selectFollowingCount" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM friend WHERE from_user_id = #{userId} AND status IN (1, 2)
    </select>

    <!-- 查询粉丝数 -->
    <select id="selectFollowerCount" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM friend WHERE to_user_id = #{userId} AND status IN (1, 2)
    </select>

    <!-- 查询好友数（双向关注） -->
    <select id="selectFriendCount" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM friend WHERE from_user_id = #{userId} AND status = 2
    </select>

    <!-- 检查是否已关注 -->
    <select id="checkIsFollowing" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM friend
        WHERE from_user_id = #{fromUserId} AND to_user_id = #{toUserId} AND status IN (1, 2)
    </select>

</mapper>
