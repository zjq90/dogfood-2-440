<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.idouban.mapper.UserMapper">

    <!-- 通用结果映射 -->
    <resultMap id="UserResultMap" type="com.idouban.entity.User">
        <id column="user_id" property="userId" jdbcType="INTEGER"/>
        <result column="username" property="username" jdbcType="VARCHAR"/>
        <result column="password" property="password" jdbcType="VARCHAR"/>
        <result column="status" property="status" jdbcType="INTEGER"/>
        <result column="reported" property="reported" jdbcType="INTEGER"/>
        <result column="title_time" property="titleTime" jdbcType="TIMESTAMP"/>
        <result column="portrait" property="portrait" jdbcType="VARCHAR"/>
        <result column="signature" property="signature" jdbcType="VARCHAR"/>
        <result column="self_introduction" property="selfIntroduction" jdbcType="VARCHAR"/>
        <result column="nickname" property="nickname" jdbcType="VARCHAR"/>
        <result column="address" property="address" jdbcType="VARCHAR"/>
        <result column="time" property="time" jdbcType="TIMESTAMP"/>
        <result column="code" property="code" jdbcType="INTEGER"/>
        <result column="out_time" property="outTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!-- 通用查询列 -->
    <sql id="Base_Column_List">
        user_id, username, password, status, reported, title_time, portrait,
        signature, self_introduction, nickname, address, time, code, out_time
    </sql>

    <!-- 根据用户名查询用户 -->
    <select id="selectByUsername" resultMap="UserResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM user
        WHERE username = #{username}
        LIMIT 1
    </select>

    <!-- 根据用户ID查询用户 -->
    <select id="selectByUserId" resultMap="UserResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM user
        WHERE user_id = #{userId}
        LIMIT 1
    </select>

    <!-- 用户登录验证 -->
    <select id="selectForLogin" resultMap="UserResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM user
        WHERE username = #{username} AND password = #{password}
        LIMIT 1
    </select>

    <!-- 新增用户（注册） -->
    <insert id="insertUser" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO user (username, password, status, nickname, portrait)
        VALUES (#{username}, #{password}, 0, #{nickname}, 'http://47.102.212.18/DouBan/image/default.png')
    </insert>

    <!-- 更新用户信息 -->
    <update id="updateUser">
        UPDATE user
        <set>
            <if test="username != null">username = #{username},</if>
            <if test="password != null">password = #{password},</if>
            <if test="status != null">status = #{status},</if>
            <if test="reported != null">reported = #{reported},</if>
            <if test="titleTime != null">title_time = #{titleTime},</if>
            <if test="portrait != null">portrait = #{portrait},</if>
            <if test="signature != null">signature = #{signature},</if>
            <if test="selfIntroduction != null">self_introduction = #{selfIntroduction},</if>
            <if test="nickname != null">nickname = #{nickname},</if>
            <if test="address != null">address = #{address},</if>
            <if test="code != null">code = #{code},</if>
            <if test="outTime != null">out_time = #{outTime},</if>
        </set>
        WHERE user_id = #{userId}
    </update>

    <!-- 更新用户头像 -->
    <update id="updatePortrait">
        UPDATE user
        SET portrait = #{portrait}
        WHERE user_id = #{userId}
    </update>

    <!-- 更新用户个人信息 -->
    <update id="updatePersonage">
        UPDATE user
        SET signature = #{signature},
            self_introduction = #{selfIntroduction},
            nickname = #{nickname},
            address = #{address}
        WHERE user_id = #{userId}
    </update>

    <!-- 更新找回密码凭证和过期时间 -->
    <update id="updateCodeAndOutTime">
        UPDATE user
        SET code = #{code},
            out_time = #{outTime}
        WHERE username = #{username}
    </update>

    <!-- 根据找回密码凭证查询用户 -->
    <select id="selectByCode" resultMap="UserResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM user
        WHERE code = #{code}
          AND out_time > NOW()
        LIMIT 1
    </select>

    <!-- 更新用户密码 -->
    <update id="updatePassword">
        UPDATE user
        SET password = #{password},
            code = NULL,
            out_time = NULL
        WHERE user_id = #{userId}
    </update>

    <!-- 查询所有用户（管理员功能） -->
    <select id="selectAllUsers" resultMap="UserResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM user
        ORDER BY time DESC
    </select>

    <!-- 根据关键词搜索用户 -->
    <select id="searchUsers" resultMap="UserResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM user
        WHERE username LIKE CONCAT('%', #{keyword}, '%')
           OR nickname LIKE CONCAT('%', #{keyword}, '%')
        ORDER BY time DESC
    </select>

    <!-- 更新用户状态（封禁/解封） -->
    <update id="updateStatus">
        UPDATE user
        SET status = #{status}
        WHERE user_id = #{userId}
    </update>

    <!-- 更新用户举报状态 -->
    <update id="updateReported">
        UPDATE user
        SET reported = #{reported}
        WHERE user_id = #{userId}
    </update>

</mapper>
