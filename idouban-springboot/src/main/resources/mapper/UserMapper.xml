<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lzh.idouban.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="com.lzh.idouban.entity.User">
        <id column="user_id" property="userId"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="status" property="status"/>
        <result column="reported" property="reported"/>
        <result column="title_time" property="titleTime"/>
        <result column="portrait" property="portrait"/>
        <result column="signature" property="signature"/>
        <result column="self_introduction" property="selfIntroduction"/>
        <result column="nickname" property="nickname"/>
        <result column="address" property="address"/>
        <result column="time" property="time"/>
        <result column="code" property="code"/>
        <result column="out_time" property="outTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        user_id, username, password, status, reported, title_time, portrait, 
        signature, self_introduction, nickname, address, time, code, out_time
    </sql>

    <!-- 根据用户名查询用户 -->
    <select id="selectByUsername" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM user
        WHERE username = #{username}
        LIMIT 1
    </select>

    <!-- 根据用户ID查询用户 -->
    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM user
        WHERE user_id = #{userId}
        LIMIT 1
    </select>

    <!-- 用户登录验证 -->
    <select id="login" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM user
        WHERE username = #{username} AND password = #{password}
        LIMIT 1
    </select>

    <!-- 检查用户名是否存在 -->
    <select id="checkUsernameExists" resultType="int">
        SELECT COUNT(1)
        FROM user
        WHERE username = #{username}
        LIMIT 1
    </select>

    <!-- 插入新用户 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO user (username, password, status, reported, portrait, 
                          signature, self_introduction, nickname, address, code, out_time)
        VALUES (#{username}, #{password}, #{status}, #{reported}, #{portrait},
                #{signature}, #{selfIntroduction}, #{nickname}, #{address}, #{code}, #{outTime})
    </insert>

    <!-- 更新用户信息 -->
    <update id="update">
        UPDATE user
        <set>
            <if test="password != null">password = #{password},</if>
            <if test="status != null">status = #{status},</if>
            <if test="reported != null">reported = #{reported},</if>
            <if test="titleTime != null">title_time = #{titleTime},</if>
            <if test="portrait != null">portrait = #{portrait},</if>
            <if test="signature != null">signature = #{signature},</if>
            <if test="selfIntroduction != null">self_introduction = #{selfIntroduction},</if>
            <if test="nickname != null">nickname = #{nickname},</if>
            <if test="address != null">address = #{address},</if>
            <if test="code != null">code = #{code},</if>
            <if test="outTime != null">out_time = #{outTime}</if>
        </set>
        WHERE user_id = #{userId}
    </update>

    <!-- 更新用户头像 -->
    <update id="updatePortrait">
        UPDATE user
        SET portrait = #{portrait}
        WHERE user_id = #{userId}
    </update>

    <!-- 更新找回密码信息 -->
    <update id="updateResetPasswordInfo">
        UPDATE user
        SET code = #{code}, out_time = #{outTime}
        WHERE user_id = #{userId}
    </update>

    <!-- 根据code查询用户 -->
    <select id="selectByCode" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM user
        WHERE code = #{code}
        LIMIT 1
    </select>

    <!-- 更新密码 -->
    <update id="updatePassword">
        UPDATE user
        SET password = #{password}, code = NULL, out_time = NULL
        WHERE user_id = #{userId}
    </update>

    <!-- 查询所有用户 -->
    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM user
        ORDER BY user_id DESC
    </select>

    <!-- 根据条件查询用户列表 -->
    <select id="selectByKeyword" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM user
        WHERE username LIKE CONCAT('%', #{keyword}, '%')
           OR nickname LIKE CONCAT('%', #{keyword}, '%')
           OR signature LIKE CONCAT('%', #{keyword}, '%')
        ORDER BY user_id DESC
    </select>

</mapper>
